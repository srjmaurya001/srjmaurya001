"use strict"; //treat all js code as newer version
// alert("dduf"); cabt use alert bcoz using nodejs not browser
let name="suraj" 
let age=25;
let isLogin=true;

// null is  standalone value 
//undefine is not assing value yet
// Symbol == unique
// object
console.log(typeof undefined);
console.log(typeof null);
